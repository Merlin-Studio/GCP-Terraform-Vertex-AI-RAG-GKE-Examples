Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# 3-services-prd — application services for Production

Generated by Merlin Studio for Cloud Foundation Fabric v58.0.0.

## Cloud Run

Deployed into `app-prd-0`.

| Service | Region | Ingress | Scaling | VPC | CMEK |
|---|---|---|---|---|---|
| `api` | europe-west4 | INGRESS_TRAFFIC_INTERNAL_LOAD_BALANCER | 1–50 | `vpc-shared-prod` | yes |

## Cloud SQL

Deployed into `dp-prd-0`.

| Instance | Engine | Tier | Availability | Private network | CMEK |
|---|---|---|---|---|---|
| `app-db` | POSTGRES_15 | db-custom-2-7680 | REGIONAL | `vpc-shared-prod` | yes |

## Deploy

1. Apply the landing-zone stages first (0-org-setup, 2-networking,
   2-security, 2-project-factory).
2. Copy this directory to `fast/stages/3-services-prd/` in the Fabric checkout
   (tag `v58.0.0`).
3. Fetch the stage tfvars and the provider file 0-org-setup published for
   `3-services-prd` (`fast-links.sh`), then:

```bash
cd fast/stages/3-services-prd
terraform init
terraform plan -out=tfplan
terraform apply tfplan
```

## Notes

- Cloud Run containers are generated with the image recorded in the review
  panel; replace it with your own before applying if it is still the
  placeholder.
- Cloud SQL reaches the VPC over private service access. The landing zone
  must have a PSA range on that VPC before this stage applies.
- The YAML files beside `main.tf` mirror it with FAST `$` tokens; keep
  them in sync when editing.

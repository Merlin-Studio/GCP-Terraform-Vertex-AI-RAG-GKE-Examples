# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

// Applying the workload stages from Jenkins. Generated by Merlin Studio for
// Cloud Foundation Fabric v58.0.0.
//
// An example, not a drop-in: credentials, agents and approval gates belong to
// your organisation. What matters is the shape. Verify the landing zone once,
// then run the stages in this order, each as the identity the landing zone
// provisioned for it, with a plan reviewed before every apply.
//
// The same sequence is in deploy/apply.sh if you would rather not use Jenkins.
pipeline {
  agent any

  parameters {
    string(name: 'FAST_CHECKOUT', defaultValue: '',
           description: 'Path to the Cloud Foundation Fabric checkout (v58.0.0) holding the deployed landing zone')
    booleanParam(name: 'PLAN_ONLY', defaultValue: true,
                 description: 'Plan every stage without applying')
  }

  options {
    timestamps()
    disableConcurrentBuilds()   // two runs would fight over the same state
  }

  stages {
    stage('Verify landing zone') {
      // Refuses to go further when the checkout is not the landing zone these
      // workloads were compiled against.
      steps {
        sh './deploy/verify.sh "${FAST_CHECKOUT}"'
      }
    }

    stage('3-artifact-registry') {
      // deploys as iac-ar
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-artifact-registry'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-artifact-registry?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-artifact-registry'
          }
        }
      }
    }

    stage('3-delivery-pools') {
      // deploys as iac-app-delivery
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-delivery-pools'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-delivery-pools?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-delivery-pools'
          }
        }
      }
    }

    stage('3-app-checkout-delivery') {
      // deploys as iac-app-delivery
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-checkout-delivery'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-checkout-delivery?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-checkout-delivery'
          }
        }
      }
    }

    stage('3-app-inventory-replica-delivery') {
      // deploys as iac-app-delivery
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-inventory-replica-delivery'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-inventory-replica-delivery?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-inventory-replica-delivery'
          }
        }
      }
    }

    stage('3-app-seller-assistant-delivery') {
      // deploys as iac-app-delivery
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-seller-assistant-delivery'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-seller-assistant-delivery?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-seller-assistant-delivery'
          }
        }
      }
    }

    stage('3-app-storefront-delivery') {
      // deploys as iac-app-delivery
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-storefront-delivery'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-storefront-delivery?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-storefront-delivery'
          }
        }
      }
    }

    stage('3-data-platform-dev') {
      // deploys as iac-dp-dev, through 2-project-factory
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-data-platform-dev'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-data-platform-dev?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-data-platform-dev'
          }
        }
      }
    }

    stage('3-gke-dev') {
      // deploys as iac-gke-dev
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-gke-dev'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-gke-dev?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-gke-dev'
          }
        }
      }
    }

    stage('3-services-dev') {
      // deploys as iac-app-dev
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-services-dev'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-services-dev?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-services-dev'
          }
        }
      }
    }

    stage('3-app-checkout-dev') {
      // deploys as iac-shop-dev
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-checkout-dev'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-checkout-dev?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-checkout-dev'
          }
        }
      }
    }

    stage('3-app-seller-assistant-dev') {
      // deploys as iac-seller-dev
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-seller-assistant-dev'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-seller-assistant-dev?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-seller-assistant-dev'
          }
        }
      }
    }

    stage('3-app-storefront-dev') {
      // deploys as iac-shop-dev
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-storefront-dev'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-storefront-dev?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-storefront-dev'
          }
        }
      }
    }

    stage('3-data-platform-stg') {
      // deploys as iac-dp-stg, through 2-project-factory
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-data-platform-stg'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-data-platform-stg?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-data-platform-stg'
          }
        }
      }
    }

    stage('3-gke-stg') {
      // deploys as iac-gke-stg
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-gke-stg'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-gke-stg?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-gke-stg'
          }
        }
      }
    }

    stage('3-services-stg') {
      // deploys as iac-app-stg
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-services-stg'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-services-stg?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-services-stg'
          }
        }
      }
    }

    stage('3-app-checkout-stg') {
      // deploys as iac-shop-stg
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-checkout-stg'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-checkout-stg?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-checkout-stg'
          }
        }
      }
    }

    stage('3-app-seller-assistant-stg') {
      // deploys as iac-seller-stg
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-seller-assistant-stg'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-seller-assistant-stg?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-seller-assistant-stg'
          }
        }
      }
    }

    stage('3-app-storefront-stg') {
      // deploys as iac-shop-stg
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-storefront-stg'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-storefront-stg?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-storefront-stg'
          }
        }
      }
    }

    stage('3-data-platform-prd') {
      // deploys as iac-dp-prd, through 2-project-factory
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-data-platform-prd'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-data-platform-prd?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-data-platform-prd'
          }
        }
      }
    }

    stage('3-gke-prd') {
      // deploys as iac-gke-prd
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-gke-prd'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-gke-prd?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-gke-prd'
          }
        }
      }
    }

    stage('3-services-prd') {
      // deploys as iac-app-prd
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-services-prd'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-services-prd?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-services-prd'
          }
        }
      }
    }

    stage('3-app-checkout-prd') {
      // deploys as iac-shop-prd
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-checkout-prd'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-checkout-prd?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-checkout-prd'
          }
        }
      }
    }

    stage('3-app-inventory-replica-prd') {
      // deploys as iac-seller-prd
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-inventory-replica-prd'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-inventory-replica-prd?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-inventory-replica-prd'
          }
        }
      }
    }

    stage('3-app-seller-assistant-prd') {
      // deploys as iac-seller-prd
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-seller-assistant-prd'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-seller-assistant-prd?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-seller-assistant-prd'
          }
        }
      }
    }

    stage('3-app-storefront-prd') {
      // deploys as iac-shop-prd
      steps {
        sh './deploy/apply.sh "${FAST_CHECKOUT}" --plan-only --stage 3-app-storefront-prd'
        script {
          if (!params.PLAN_ONLY) {
            input message: 'Apply 3-app-storefront-prd?', ok: 'Apply'
            sh './deploy/apply.sh "${FAST_CHECKOUT}" --stage 3-app-storefront-prd'
          }
        }
      }
    }
  }

  post {
    failure {
      echo 'A stage failed. Later stages did not run, so the landing zone is ' +
           'partly applied; fix the cause and re-run from the failed stage with --stage.'
    }
  }
}

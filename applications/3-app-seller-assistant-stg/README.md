Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# 3-app-seller-assistant-stg

The **seller-assistant** application in `stg`, as archetype AI2: retrieval-augmented generation.

## What this stage builds

- `seller-assistant-objects-stg` — gcs buckets in `seller-dp-stg-0`
- `seller-assistant-embeddings-stg` — cloud run services in `seller-app-stg-0`
- `seller-assistant-vectors-stg` — vector indexes in `seller-dp-stg-0`
- `seller-assistant-internet-stg` — front doors in `seller-app-stg-0`
- `seller-assistant-api-stg` — cloud run services in `seller-app-stg-0`
- `seller-assistant-llm-stg` — model guardrails in `seller-app-stg-0`
- `seller-assistant-events-stg` — pubsub topics in `seller-dp-stg-0`

## Why each grant exists

| Identity | Role | On | Because of |
|---|---|---|---|
| `seller-assistant-embedding-stg` | `roles/storage.objectViewer` | `seller-assistant-objects-stg` | `objects -> embeddings` |
| `seller-assistant-embedding-stg` | `roles/aiplatform.user` | `seller-assistant-vectors-stg` | `embeddings -> vectors` |
| `seller-assistant-embedding-stg` | `roles/pubsub.subscriber` | `seller-assistant-events-stg` | `events -> embeddings` |
| `seller-assistant-embedding-stg` | `roles/pubsub.viewer` | `seller-assistant-events-stg` | `events -> embeddings` |
| `seller-assistant-api-stg` | `roles/aiplatform.user` | `seller-assistant-vectors-stg` | `api -> vectors` |
| `seller-assistant-api-stg` | `roles/aiplatform.user` | `seller-assistant-llm-stg` | `api -> llm` |

## What Merlin did not produce

This stage is the platform an application lands on, not the application. You still own:

- **objects**: object layout, file contents
- **embeddings**: chunking strategy, embedding model choice, reindex policy
- **vectors**: corpus and chunking, embedding model choice, retrieval evaluation
- **internet**: dns records, certificate ownership, firewall rule content
- **api**: application code, routes and handlers, runtime configuration
- **llm**: model choice, prompt content, evaluation and grounding
- **events**: message schema, message contents

The container image is a placeholder until your pipeline publishes one, and the database secret is an empty container: the value is added out of band, so it never passes through a specification, a bundle or a state file.

## Before you apply

Run `deploy/preflight-seller-assistant-stg.sh` first. In your own environment it checks that:

- the projects exist
- the VPC, subnet and Private Service Access range exist in the Shared VPC host
- each project is attached to that host
- every landing-zone key this stage names exists where the landing zone keeps it
- every firewall policy a front door attaches exists in its project
- the services the stage uses are enabled

and names the assumption and the resource when one does not hold. What it cannot see from outside -- that your deploying identity may use every one of these -- `terraform plan` finds next.

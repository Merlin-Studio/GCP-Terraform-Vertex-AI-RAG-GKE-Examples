Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# 3-app-checkout-stg

The **checkout** application in `stg`, as archetype A3: api with event analytics.

## What this stage builds

- `checkout-internet-stg` — front doors in `shop-app-stg-0`
- `checkout-api-stg` — cloud run services in `shop-app-stg-0`
- `checkout-relational-stg` — cloud sql instances in `shop-dp-stg-0`
- `checkout-events-stg` — pubsub topics in `shop-dp-stg-0`
- `checkout-stream-processing-stg` — dataflow scaffolds in `shop-dp-stg-0`
- `checkout-warehouse-stg` — bigquery datasets in `shop-dp-stg-0`
- `checkout-db-stg` — secret manager secrets in `shop-app-stg-0`, derived: `api -> relational` needs it
- `checkout-pipeline-stg` — gcs buckets in `shop-dp-stg-0`, derived: `events -> stream-processing` needs it

## Why each grant exists

| Identity | Role | On | Because of |
|---|---|---|---|
| `checkout-api-stg` | `roles/cloudsql.client` | `checkout-relational-stg` | `api -> relational` |
| `checkout-api-stg` | `roles/cloudsql.instanceUser` | `checkout-relational-stg` | `api -> relational` |
| `checkout-api-stg` | `roles/secretmanager.secretAccessor` | `checkout-db-stg` | `api -> relational` |
| `checkout-api-stg` | `roles/pubsub.publisher` | `checkout-events-stg` | `api -> events` |
| `checkout-stream-processing-stg` | `roles/pubsub.subscriber` | `checkout-events-stg` | `events -> stream-processing` |
| `checkout-stream-processing-stg` | `roles/pubsub.viewer` | `checkout-events-stg` | `events -> stream-processing` |
| `checkout-stream-processing-stg` | `roles/dataflow.worker` | `shop-dp-stg-0` | `events -> stream-processing` |
| `checkout-stream-processing-stg` | `roles/storage.objectAdmin` | `checkout-pipeline-stg` | `events -> stream-processing` |
| `checkout-stream-processing-stg` | `roles/bigquery.dataEditor` | `checkout-warehouse-stg` | `stream-processing -> warehouse` |
| `checkout-stream-processing-stg` | `roles/bigquery.jobUser` | `shop-dp-stg-0` | `stream-processing -> warehouse` |

## What Merlin did not produce

This stage is the platform an application lands on, not the application. You still own:

- **internet**: dns records, certificate ownership, firewall rule content
- **api**: application code, routes and handlers, runtime configuration
- **relational**: schema and migrations, extra users and engine flags
- **events**: message schema, message contents
- **stream-processing**: pipeline code, transforms and windowing, schema mapping
- **warehouse**: schema and tables, views and queries

The container image is a placeholder until your pipeline publishes one, and the database secret is an empty container: the value is added out of band, so it never passes through a specification, a bundle or a state file.

## What costs money from the first apply

A streaming pipeline starts when this stage is applied and runs until it is stopped. Its workers are billed while they run, whether or not any message arrives, so this line on the bill begins before the application has a single user. That is deliberate -- a pipeline that waits for someone to start it is a backlog nobody noticed -- but it is a decision, so here it is.

| Pipeline | Workers | Machine | Runs |
|---|---|---|---|
| `checkout-stream-processing-stg` | at least 1, up to 2 | `n1-standard-2` | continuously, from apply |

To hold the cost until the application is live, apply this stage without the pipeline (`-target` the rest) or drain the job after the first apply; the budget in `operations.tf` warns at half, most and all of its amount.

## Before you apply

Run `deploy/preflight-checkout-stg.sh` first. In your own environment it checks that:

- the projects exist
- the VPC, subnet and Private Service Access range exist in the Shared VPC host
- each project is attached to that host
- every landing-zone key this stage names exists where the landing zone keeps it
- every firewall policy a front door attaches exists in its project
- the services the stage uses are enabled

and names the assumption and the resource when one does not hold. What it cannot see from outside -- that your deploying identity may use every one of these -- `terraform plan` finds next.

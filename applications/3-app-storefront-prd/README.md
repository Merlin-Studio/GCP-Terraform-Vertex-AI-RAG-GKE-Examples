Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# 3-app-storefront-prd

The **storefront** application in `prd`, as archetype K1: microservices on the shared cluster.

## What this stage builds

- `storefront-internet-prd` — front doors in `shop-app-prd-0`
- `storefront-services-prd` — gke workloads in `shop-app-prd-0`
- `storefront-relational-prd` — cloud sql instances in `shop-dp-prd-0`
- `storefront-events-prd` — pubsub topics in `shop-dp-prd-0`
- `storefront-cache-prd` — memorystore instances in `shop-dp-prd-0`
- `storefront-db-prd` — secret manager secrets in `shop-app-prd-0`, derived: `services -> relational` needs it

## Why each grant exists

| Identity | Role | On | Because of |
|---|---|---|---|
| `storefront-services-prd` | `roles/cloudsql.client` | `storefront-relational-prd` | `services -> relational` |
| `storefront-services-prd` | `roles/cloudsql.instanceUser` | `storefront-relational-prd` | `services -> relational` |
| `storefront-services-prd` | `roles/secretmanager.secretAccessor` | `storefront-db-prd` | `services -> relational` |
| `storefront-services-prd` | `roles/pubsub.publisher` | `storefront-events-prd` | `services -> events` |
| `storefront-services-prd` | `roles/redis.viewer` | `storefront-cache-prd` | `services -> cache` |

## What Merlin did not produce

This stage is the platform an application lands on, not the application. You still own:

- **internet**: dns records, certificate ownership, firewall rule content
- **services**: application code, routes and handlers, runtime configuration
- **relational**: schema and migrations, extra users and engine flags
- **events**: message schema, message contents
- **cache**: eviction policy, cached contents

The container image is a placeholder until your pipeline publishes one, and the database secret is an empty container: the value is added out of band, so it never passes through a specification, a bundle or a state file.

## Before you apply

Run `deploy/preflight-storefront-prd.sh` first. In your own environment it checks that:

- the projects exist
- the VPC, subnet and Private Service Access range exist in the Shared VPC host
- each project is attached to that host
- every landing-zone key this stage names exists where the landing zone keeps it
- every firewall policy a front door attaches exists in its project
- the cluster exists, and the accelerator pool when a model asks for one
- the services the stage uses are enabled

and names the assumption and the resource when one does not hold. What it cannot see from outside -- that your deploying identity may use every one of these -- `terraform plan` finds next.

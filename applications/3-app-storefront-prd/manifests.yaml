# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# Kubernetes manifests for storefront in prd.
#
# The cluster is not this application's: the landing zone builds one per
# environment, and an application takes a namespace in it. That separation is
# what stops every application from owning a cluster it cannot operate.
#
# Apply these with the cluster's own delivery path -- Config Sync, or kubectl
# against the credentials the GKE stage set up. The Terraform beside this file
# creates the Google identities these accounts impersonate, and must be
# applied first: a pod that authenticates as an identity nobody created gets
# a permission error nobody can explain.
---
apiVersion: v1
kind: Namespace
metadata:
  name: storefront-prd
  labels:
    application: storefront
    environment: prd
    managed-by: merlin
---
# --- storefront-services-prd -------------------------------------------------
apiVersion: v1
kind: ServiceAccount
metadata:
  name: storefront-services-prd
  namespace: storefront-prd
  annotations:
    # Workload Identity: this account impersonates the Google one, so every
    # grant in the Terraform beside this file lands on a Google identity and
    # no key is ever written into a secret.
    iam.gke.io/gcp-service-account: storefront-services-prd@vd-shop-app-prd-0.iam.gserviceaccount.com
  labels:
    application: storefront
    managed-by: merlin
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: storefront-services-prd
  namespace: storefront-prd
  labels:
    app: storefront-services-prd
    application: storefront
    managed-by: merlin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: storefront-services-prd
  template:
    metadata:
      labels:
        app: storefront-services-prd
        application: storefront
    spec:
      serviceAccountName: storefront-services-prd
      # The default token is not mounted; this pod authenticates through
      # Workload Identity and needs no Kubernetes API access to do its job.
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: app
          # A placeholder until your pipeline publishes one. The delivery
          # stage's skaffold example names this slot.
          image: us-docker.pkg.dev/cloudrun/container/hello
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              cpu: "1"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "1Gi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: storefront-services-prd
  namespace: storefront-prd
  labels:
    app: storefront-services-prd
spec:
  type: ClusterIP
  selector:
    app: storefront-services-prd
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
# --- Network policy ------------------------------------------------------
# Deny everything, then allow what the arrows of the graph actually need. A
# namespace nothing reaches is reachable by nothing, which is the state a
# cluster should start from rather than arrive at.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: storefront-prd
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: storefront-services-prd-allows
  namespace: storefront-prd
spec:
  podSelector:
    matchLabels:
      app: storefront-services-prd
  policyTypes: [Ingress, Egress]
  ingress:
    # Reached from outside the cluster, so the gateway may reach it.
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: gke-gateway-system
      ports:
        - protocol: TCP
          port: 8080
  egress:
    # DNS, or nothing resolves and every other rule is academic.
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # events: reached at Google's API
    # endpoint over Private Google Access, which is a published address range
    # rather than a pod.
    - to:
        - ipBlock:
            cidr: 199.36.153.8/30
      ports:
        - protocol: TCP
          port: 443
    # cache, relational: reached at a private
    # address inside the VPC, not through Google's API endpoint.
    # A managed database or cache answers on a Private Service Access range
    # the landing zone allocated, which Merlin cannot see from a
    # specification. Add that range here; the preflight already checks the
    # allocation exists before you apply.
    # - to:
    #     - ipBlock:
    #         cidr: <your Private Service Access range>
---
# --- Quota ---------------------------------------------------------------
# A ceiling per namespace, so one application cannot starve the cluster it
# shares with the others.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: storefront-quota
  namespace: storefront-prd
spec:
  hard:
    requests.cpu: "20"
    requests.memory: "20Gi"
    pods: "40"
---
# --- Gateway -------------------------------------------------------------
# One gateway for the application. Each member is reached by path, which is
# what makes several services one application rather than several.
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: storefront
  namespace: storefront-prd
spec:
  gatewayClassName: gke-l7-global-external-managed
  listeners:
    - name: https
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        options:
          # A placeholder certificate until you own the domain; the README
          # and the preflight both say to replace it.
          networking.gke.io/pre-shared-certs: storefront-cert
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: storefront
  namespace: storefront-prd
spec:
  parentRefs:
    - name: storefront
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: storefront-services-prd
          port: 80
---
# The web application firewall, on what the gateway sends traffic to. A
# Gateway has no policy of its own: GKE attaches it to each backend service.
apiVersion: networking.gke.io/v1
kind: GCPBackendPolicy
metadata:
  name: storefront-services-prd
  namespace: storefront-prd
spec:
  default:
    securityPolicy: compliance-waf-policy
    logging:
      enabled: true
      sampleRate: 1000000
  targetRef:
    group: ""
    kind: Service
    name: storefront-services-prd

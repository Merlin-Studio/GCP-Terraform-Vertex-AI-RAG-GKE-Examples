Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# 3-gke-prd — GKE clusters for Production

Generated by Merlin Studio for Cloud Foundation Fabric v58.0.0.

| Cluster | Type | Location | VPC / subnet | Node pools | CMEK |
|---|---|---|---|---|---|
| `gke-prod` | standard | europe-west4 | `vpc-shared-prod` / `sb-prod-europe-west4` | gke-prod-pool | yes |

## Deploy

1. Apply the landing-zone stages first (0-org-setup, 2-networking, 2-security,
   2-project-factory).
2. Copy this directory to `fast/stages/3-gke-prd/` in the Fabric checkout
   (tag `v58.0.0`).
3. Fetch the provider/backend file and the stage tfvars published by 0-org-setup,
   2-networking and 2-security (`fast-links.sh`), then:

```bash
cd fast/stages/3-gke-prd
terraform init
terraform plan -out=tfplan
terraform apply tfplan
```

## Notes

- `clusters/*.yaml` and `nodepools/*/*.yaml` mirror `main.tf` with FAST `$` tokens;
  keep them in sync when editing.
- The ranges allowed to reach the control plane are `var.corporate_ranges`. Where
  the landing zone routes to on-premises ranges, `corporate.auto.tfvars` pre-fills
  them; otherwise supply them at plan time. `deploy/preflight-gke-<env>.sh` checks.
- Namespaces listed in the cluster YAML are informational until the Kubernetes
  provider stage is added.

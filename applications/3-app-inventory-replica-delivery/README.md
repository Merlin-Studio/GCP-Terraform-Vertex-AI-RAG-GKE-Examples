Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# 3-app-inventory-replica-delivery

How **inventory-replica**'s code reaches the slots the other stages built.

## What this stage builds

- A container repository shared by every environment, because an image promoted between them has to be the same image.
- A delivery pipeline that promotes prd.

| Target | Approval |
|---|---|
| `inventory-replica-prd` | a person approves before a release lands |

## What Merlin did not produce

The build. A delivery pipeline needs to know how to turn this application's source into an image and which manifest to apply, and that lives in the application's repository rather than in its landing zone. `skaffold.example.yaml` beside this file is an example, and its first line says so.

The slots it has to name are:

- `inventory-replica-job`

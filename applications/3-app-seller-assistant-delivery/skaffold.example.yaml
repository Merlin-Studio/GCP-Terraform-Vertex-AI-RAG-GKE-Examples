# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# EXAMPLE, not a working configuration.
#
# Cloud Deploy needs to know how to turn this application's source
# into an image and which manifest to apply. That is the
# application's, not its landing zone's. What Merlin can tell you is
# the name of every slot the stages built, which is what the
# manifests below have to name.

apiVersion: skaffold/v4beta7
kind: Config
metadata:
  name: seller-assistant
profiles:
  - name: dev
    manifests:
      rawYaml:
        # - manifests/dev.yaml
  - name: stg
    manifests:
      rawYaml:
        # - manifests/stg.yaml
  - name: prd
    manifests:
      rawYaml:
        # - manifests/prd.yaml

# The slots this application deploys into:
#   seller-assistant-api
#   seller-assistant-embeddings

Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# 3-artifact-registry — Artifact Registry

Generated by Merlin Studio for Cloud Foundation Fabric v58.0.0.

Repositories are shared by every environment, so this stage has no
environment suffix. It deploys into `app-core-0`, the shared
application project the landing zone creates.

| Repository | Format | Location | Keep versions | CMEK |
|---|---|---|---|---|
| `containers` | DOCKER | europe-west4 | 10 | yes |

## Deploy

1. Apply the landing-zone stages first (0-org-setup, 2-project-factory).
2. Copy this directory to `fast/stages/3-artifact-registry/` in the Fabric checkout
   (tag `v58.0.0`).
3. Fetch the provider file 0-org-setup published for `3-artifact-registry`, then:

```bash
cd fast/stages/3-artifact-registry
terraform init
terraform plan -out=tfplan
terraform apply tfplan
```

# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# Egress: Centralized logging to sinks outside perimeter
#
# Compliance:
#   ISO 27001: A.8.15 Logging — ensure event logs are delivered to centralized storage
#   GDPR: Art. 30 Records of processing — ensure processing activity logs are collected centrally
#   CIS Benchmarks: CIS GCP 2.x Logging — ensure log sinks deliver to centralized destinations
#   NIS2 Directive: Art. 21(2)(b) Incident handling — centralized log collection for detection
#
# Without this, log delivery to centralized sinks fails silently.

from:
  identity_type: ANY_IDENTITY
to:
  operations:
  - method_selectors:
    - google.logging.v2.LoggingServiceV2.WriteLogEntries
    - google.logging.v2.LoggingServiceV2.CreateLogEntry
    service_name: logging.googleapis.com
  - method_selectors:
    - google.cloud.bigquery.v2.TableDataService.InsertAll
    service_name: bigquery.googleapis.com
  resources:
  - '*'
